# requirements.txt
# Зависимости для приложения каталога автозапчастей

# Основной фреймворк веб-приложения
streamlit>=1.38.0,<2.0.0

# Работа с данными
pandas>=2.2.0,<3.0.0
polars>=1.0.0,<2.0.0

# База данных
duckdb>=1.0.0,<2.0.0

# Работа с файлами Excel
openpyxl>=3.1.0,<4.0.0  # Для записи Excel

# Чтение Excel через Polars (использует fastexcel под капотом)
fastexcel>=0.9.0

# Работа с файлами CSV/Parquet (обычно входят в состав pandas/polars, но можно указать явно)
pyarrow>=16.0.0,<20.0.0  # Для поддержки Arrow в Polars/DuckDB
